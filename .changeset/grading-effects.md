---
'ditherette': minor
---

Add grading effects: `curves`, `brightness-contrast`, `exposure`, `white-balance`, and `hue-saturation`. They compose with `levels` in any order through `applyEffects` and recipe version 2.
